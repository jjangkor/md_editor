{
  "name": "jjang-md-editor",
  "version": "1.0.0",
  "private": true,
  "description": "Regression checks for the standalone Markdown editor",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "26.1.0",
    "marked": "11.1.1"
  }
}
